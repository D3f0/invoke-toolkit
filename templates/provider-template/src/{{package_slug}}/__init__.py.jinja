"""{{ project_description }}."""

from collections.abc import Mapping, Sequence
from typing import Any

from invoke_toolkit import Context, FieldResolutionRequest


def resolve(
    ctx: Context,
    requests: Sequence[FieldResolutionRequest],
) -> Mapping[str, Any]:
    """Resolve one batch of {{ provider_scheme }} URI field references.

    Return exactly one final value per requested parameter. Use ``ctx`` for
    consumer configuration and ``request.annotation`` when provider behavior
    depends on the requested runtime type.
    """
    del ctx, requests
    raise NotImplementedError(
        "Implement resolve() to resolve {{ provider_scheme }} field references."
    )


__all__ = ["resolve"]
